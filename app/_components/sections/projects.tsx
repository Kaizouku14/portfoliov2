import { MENU } from "@/constants/menu-options";

const Projects = () => {
  return (
    <section id={MENU.PROJECTS} className="h-screen">
      <h1>Projects</h1>
    </section>
  );
};

export default Projects;
