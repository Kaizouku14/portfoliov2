import { MENU_ITEMS } from "@/constants/menu-options";

const Contact = () => {
  return (
    <section id={MENU_ITEMS.CONTACT} className="h-120 scroll-mt-20">
      <h1>Contact</h1>
    </section>
  );
};

export default Contact;
