import { MENU_ITEMS } from "@/constants/menu-options";

const Projects = () => {
  return (
    <section id={MENU_ITEMS.PROJECTS} className="h-120 scroll-mt-20">
      <h1>Projects</h1>
    </section>
  );
};

export default Projects;
