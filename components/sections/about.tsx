import { MENU } from "@/constants/menu-options";

const About = () => {
  return (
    <section id={MENU.ABOUT} className="h-screen">
      <h1>About</h1>
    </section>
  );
};

export default About;
