import { MENU_ITEMS } from "@/constants/menu-options";

const Home = () => {
  return (
    <section id={MENU_ITEMS.HOME} className="h-120 scroll-mt-20">
      <h1>Home</h1>
    </section>
  );
};

export default Home;
